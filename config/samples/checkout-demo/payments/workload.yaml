apiVersion: apps/v1
kind: Deployment
metadata:
  name: payments-svc
  namespace: checkout
  labels:
    app: payments-svc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: payments-svc
  template:
    metadata:
      labels:
        app: payments-svc
    spec:
      containers:
        - name: app
          image: stefanprodan/podinfo:6.5.3
          ports:
            - name: http
              containerPort: 9898
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
          resources:
            requests:
              cpu: 10m
              memory: 32Mi
            limits:
              cpu: 100m
              memory: 64Mi
---
apiVersion: v1
kind: Service
metadata:
  name: payments-svc
  namespace: checkout
  labels:
    app: payments-svc
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 9898
      targetPort: http
  selector:
    app: payments-svc
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: payments-svc
  namespace: checkout
  labels:
    app: payments-svc
spec:
  jobLabel: app
  selector:
    matchLabels:
      app: payments-svc
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
