---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: servicelevelobjectives.observability.slok.io
spec:
  group: observability.slok.io
  names:
    kind: ServiceLevelObjective
    listKind: ServiceLevelObjectiveList
    plural: servicelevelobjectives
    shortNames:
    - slo
    singular: servicelevelobjective
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: Display Name
      type: string
    - jsonPath: .status.objective.status
      name: Status
      type: string
    - format: float
      jsonPath: .status.objective.actual
      name: Actual
      type: number
    - format: float
      jsonPath: .status.objective.target
      name: Target
      type: number
    - format: float
      jsonPath: .status.objective.errorBudget.percentRemaining
      name: Budget %
      type: number
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ServiceLevelObjective is the Schema for the servicelevelobjectives
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of ServiceLevelObjective
            properties:
              displayName:
                description: displayName is the human-readable name for the Service
                  Level Objective.
                maxLength: 255
                minLength: 1
                type: string
              objective:
                description: objectives defines the objective for this Service Level
                  Objective.
                properties:
                  alerting:
                    description: alerting configures alerting rules (budget and burn
                      rate) for this objective.
                    properties:
                      budgetErrorAlerts:
                        description: budgetAlerts is a list of error budget threshold
                          alerts.
                        properties:
                          alerts:
                            description: |-
                              alerts is a list of budget alerting rules.
                              Each alert specifies a threshold percentage and severity level.
                            items:
                              properties:
                                name:
                                  description: name is the unique identifier for this
                                    budget alert.
                                  minLength: 1
                                  type: string
                                percent:
                                  description: percent is the error budget remaining
                                    threshold below which the alert fires (e.g., 10.0
                                    means < 10%).
                                  maximum: 100
                                  minimum: 0
                                  type: number
                                severity:
                                  description: severity is the alert severity level
                                    (e.g., "critical", "warning").
                                  enum:
                                  - critical
                                  - warning
                                  - info
                                  type: string
                              required:
                              - name
                              - percent
                              - severity
                              type: object
                            type: array
                          enabled:
                            type: boolean
                        type: object
                      burnRateAlerts:
                        description: burnRateAlerts is a list of burn rate alerting
                          rules.
                        properties:
                          alerts:
                            description: name is the unique identifier for this burn
                              rate alert.
                            items:
                              properties:
                                consumePercent:
                                  description: |-
                                    consumePercent is the percentage of error budget that would be consumed
                                    within the consumeWindow at the current burn rate to trigger the alert (e.g., 2.0 means 2%).
                                  minimum: 0
                                  type: number
                                consumeWindow:
                                  description: consumeWindow is the time window used
                                    to compute the burn rate threshold (e.g., "1h",
                                    "6h").
                                  pattern: ^(\d+d|\d+h|\d+m|\d+s)$
                                  type: string
                                longWindow:
                                  description: longWindow is the long observation
                                    window for the burn rate query (e.g., "1h").
                                  pattern: ^(\d+d|\d+h|\d+m|\d+s)$
                                  type: string
                                name:
                                  minLength: 1
                                  type: string
                                severity:
                                  description: severity is the alert severity level
                                    (e.g., "critical", "warning").
                                  enum:
                                  - critical
                                  - warning
                                  - info
                                  type: string
                                shortWindow:
                                  description: shortWindow is the short observation
                                    window for the burn rate query (e.g., "5m").
                                  pattern: ^(\d+d|\d+h|\d+m|\d+s)$
                                  type: string
                              required:
                              - consumePercent
                              - consumeWindow
                              - longWindow
                              - name
                              - severity
                              - shortWindow
                              type: object
                            type: array
                          enabled:
                            type: boolean
                        type: object
                    type: object
                  name:
                    description: name is the unique name of the objective within the
                      Service Level Objective.
                    maxLength: 100
                    minLength: 1
                    type: string
                  sli:
                    description: sli defines the Service Level Indicator used to measure
                      this objective.
                    properties:
                      query:
                        description: |-
                          query contains the manual Prometheus metric selectors.
                          Required if template is not specified.
                        properties:
                          errorQuery:
                            description: |-
                              errorQuery is the Prometheus metric selector for error events.
                              Example: http_requests_total{service="api",status=~"5.."}
                            type: string
                          totalQuery:
                            description: |-
                              totalQuery is the Prometheus metric selector for total events.
                              Example: http_requests_total{service="api"}
                            type: string
                        type: object
                      template:
                        description: |-
                          template specifies a predefined SLI template.
                          When set, query fields are ignored.
                        properties:
                          labels:
                            additionalProperties:
                              type: string
                            description: |-
                              labels are the Prometheus label selectors to filter metrics.
                              Example: {"service": "payment-api", "namespace": "production"}
                            type: object
                          name:
                            description: |-
                              name is the name of the template to use for this SLI.
                              Available templates: http-availability, http-latency, kubernetes-apiserver
                            enum:
                            - http-availability
                            - http-latency
                            - kubernetes-apiserver
                            type: string
                          params:
                            additionalProperties:
                              type: string
                            description: |-
                              params are template-specific parameters.
                              For http-latency: threshold (e.g., "0.5" for 500ms)
                            type: object
                        type: object
                    type: object
                  target:
                    description: target is the target percentage for the objective
                      (e.g., 99.9).
                    maximum: 200
                    minimum: 0
                    type: number
                  window:
                    description: window is the time window over which the objective
                      is measured (e.g., "30d" for 30 days).
                    type: string
                required:
                - name
                - sli
                - target
                type: object
            required:
            - displayName
            - objective
            type: object
          status:
            description: status defines the observed state of ServiceLevelObjective
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the ServiceLevelObjective resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastUpdateTime:
                description: lastUpdateTime indicates the last time the status was
                  updated.
                format: date-time
                type: string
              objective:
                description: objectives represent the current status of each objective
                  defined in the spec.
                properties:
                  actual:
                    description: actual is the current SLI percentage (e.g., 99.87).
                    type: number
                  burnRate:
                    description: burnRate contains the observed burn rate metrics.
                    items:
                      description: BurnRateStatus represents the observed burn rate
                        for an objective.
                      properties:
                        longBurnRate:
                          description: longBurnRate is the burn rate computed over
                            the long observation window.
                          type: number
                        longWindow:
                          description: longWindow is the duration of the long observation
                            window (e.g., "1h").
                          type: string
                        shortBurnRate:
                          description: shortBurnRate is the burn rate computed over
                            the short observation window.
                          type: number
                        shortWindow:
                          description: shortWindow is the duration of the short observation
                            window (e.g., "5m").
                          type: string
                      required:
                      - longBurnRate
                      - longWindow
                      - shortBurnRate
                      - shortWindow
                      type: object
                    type: array
                  errorBudget:
                    description: errorBudget contains details about error budget consumption.
                    properties:
                      consumed:
                        description: Consumed error budget so far (e.g., "10.5m")
                        type: string
                      percentRemaining:
                        description: PercentRemaining is the percentage of budget
                          left (e.g., 75.69)
                        type: number
                      remaining:
                        description: Remaining error budget (e.g., "32.7m")
                        type: string
                      total:
                        description: Total error budget for the window (e.g., "43.2m"
                          for 43.2 minutes)
                        type: string
                    required:
                    - consumed
                    - percentRemaining
                    - remaining
                    - total
                    type: object
                  lastQueried:
                    description: lastQueried is the timestamp of the last Prometheus
                      query for this objective.
                    format: date-time
                    type: string
                  name:
                    description: name of the objective (matches Objective.Name).
                    type: string
                  status:
                    description: status indicates whether the objective is being met.
                    enum:
                    - met
                    - warning
                    - degraded
                    - critical
                    - violated
                    - unknown
                    type: string
                  target:
                    description: target percentage (copied from spec for convenience).
                    type: number
                required:
                - actual
                - errorBudget
                - name
                - status
                - target
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
